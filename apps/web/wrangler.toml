name = "cloudtify-web"
pages_build_output_dir = "./out"
compatibility_date = "2025-01-01"

# Pages Functions secrets/vars are managed via `wrangler pages secret put`
# (SUPABASE_URL, SUPABASE_ANON_KEY, R2_ACCOUNT_ID, R2_BUCKET_NAME,
# R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY).
