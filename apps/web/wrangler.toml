name = "cloudtify-web"
compatibility_date = "2025-01-01"

[env.production]
name = "cloudtify-web-prod"

# Cloudflare Pages deployment
# Deploy via: wrangler pages deploy ./out
# Or connect GitHub repo in Cloudflare Dashboard

[vars]
NEXT_PUBLIC_APP_URL = "https://cloudtify.com"
